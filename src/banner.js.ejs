/*!
    <%= name %><% if (description) { %> -- <%= description %><% } %><% if (version) { %>
    Version <%= version %><% } %><% if (homepage || url) { %>
    <%= homepage || url %><% } %>
    (c) <%= year %> <%= author %>, <%= licence || license %>
*/
